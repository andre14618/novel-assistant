DEEPSEEK_API_KEY=sk-...

# Optional OpenAI-compatible endpoint override (for the tailnet AI rig):
# LLM_API_URL=http://rig.tailae3a06.ts.net:8080/v1/chat/completions
# LLM_MODEL=Qwen3.8
# LLM_API_KEY=local-tailnet

# Offline mode: llm.ts returns canned completions with fake usage so the
# telemetry path (db write + console cost line) can be exercised without
# network or a key. Used by `bun run smoke`.
LLM_OFFLINE=0
